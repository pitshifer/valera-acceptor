version: 2.1
jobs:
  build:
    docker:
     - image: alpine:3.7
    steps:
      - run:
          name: The first step
          command: |
            echo 'H W !'
            echo 'This is the delivery pipeline'
